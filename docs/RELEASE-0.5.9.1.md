# rtxForge 0.5.9.1

Use rtxForge consistently in the application, launcher, and package name. Show the application version in the main header so the running build is immediately identifiable. Includes all 0.5.9 UI changes and the install/reset hotfix.
