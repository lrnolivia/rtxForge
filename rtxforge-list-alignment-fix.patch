--- a/gui/rtxforge_gtk.py
+++ b/gui/rtxforge_gtk.py
@@ -1203,13 +1203,24 @@
     box-shadow: none;
 }
 
-/* RTXFORGE_LIST_COMPACT_V2 */
+/* RTXFORGE_LIST_COMPACT_V3
+ *
+ * The row owns all outer padding. Interior game identity uses a simple
+ * 10px rhythm: checkbox -> artwork -> title.
+ */
 .library-list-row {
-    min-height: 58px;
+    min-height: 0;
+    padding: 5px 10px;
 }
 
 .library-list-game {
-    padding: 3px 4px;
+    padding: 0;
+}
+
+.library-list-thumb {
+    margin: 0;
+    border: 2px solid transparent;
+    border-radius: 10px;
 }
 
 .library-list-title {
@@ -1232,33 +1243,16 @@
 }
 
 .library-list-actions {
-    padding-right: 2px;
-}
-
-.library-list-thumb {
-    border: 2px solid transparent;
-    border-radius: 10px;
-}
-
-
-.accent-picker-art-frame {
-    padding: 3px;
-    border: 3px solid alpha(@window_fg_color,0.22);
-    border-radius: 20px;
-    background: alpha(black,0.18);
-}
-
-.accent-picker-art-clip {
-    border-radius: 16px;
-    background: black;
-}
-
-.accent-picker-actions {
-    padding-top: 2px;
-}
-
-.accent-picker-actions button {
-    min-width: 86px;
+    padding: 0;
+}
+
+.library-list-actions button {
+    min-height: 30px;
+}
+
+/* Match row content edges exactly so header columns line up below it. */
+.library-list-header {
+    padding: 0 10px 6px;
 }
 
 .game-card .poster-fallback {
@@ -2864,7 +2858,8 @@
         )
 
         for column,(title,width,expand) in enumerate((
-            ('Game',360,True),
+            # Game is the ONLY flexible column.
+            ('Game',1,True),
             ('Location',150,False),
             ('Status',105,False),
             ('Enhancements',120,False),
@@ -5767,11 +5762,14 @@
             )
             row.set_size_request(
                 -1,
-                58,
+                -1,
+            )
+            row.set_valign(
+                Gtk.Align.START
             )
 
             game_cell=Gtk.Box(
-                spacing=0,
+                spacing=10,
                 hexpand=True,
                 valign=Gtk.Align.CENTER,
             )
@@ -5779,8 +5777,11 @@
                 'library-list-game'
             )
             game_cell.set_size_request(
-                360,
+                1,
                 -1,
+            )
+            game_cell.set_halign(
+                Gtk.Align.FILL
             )
 
             # List uses the same horizontal artwork family as
@@ -5864,10 +5865,10 @@
                 valign=Gtk.Align.CENTER,
             )
             check.set_margin_start(
-                6
+                0
             )
             check.set_margin_end(
-                6
+                0
             )
             check.set_tooltip_text(
                 'Select '+game['name']
@@ -5884,7 +5885,7 @@
                 valign=Gtk.Align.CENTER,
             )
             game_text.set_margin_start(
-                10
+                0
             )
 
             title=label(
@@ -5996,6 +5997,15 @@
             location_source.set_halign(
                 Gtk.Align.START
             )
+            location_source.set_width_chars(
+                1
+            )
+            location_source.set_max_width_chars(
+                1
+            )
+            location_source.set_hexpand(
+                False
+            )
             location=label(
                 location_text,
                 'library-list-location',
@@ -6004,10 +6014,16 @@
                 game_path
             )
             location.set_halign(
-                Gtk.Align.START
+                Gtk.Align.FILL
             )
             location.set_hexpand(
                 True
+            )
+            location.set_width_chars(
+                1
+            )
+            location.set_max_width_chars(
+                1
             )
             location.set_single_line_mode(
                 True
@@ -6066,6 +6082,15 @@
                 status_text,
                 'library-list-status-text',
             )
+            status_label.set_width_chars(
+                1
+            )
+            status_label.set_max_width_chars(
+                1
+            )
+            status_label.set_ellipsize(
+                Pango.EllipsizeMode.END
+            )
             status_primary.append(
                 status_label
             )
@@ -6078,6 +6103,15 @@
             )
             test_label.set_halign(
                 Gtk.Align.START
+            )
+            test_label.set_width_chars(
+                1
+            )
+            test_label.set_max_width_chars(
+                1
+            )
+            test_label.set_ellipsize(
+                Pango.EllipsizeMode.END
             )
             status_box.append(
                 test_label
